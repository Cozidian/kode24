Run `mix precommit` and report the results. This alias runs: compile (warnings as errors), removes unused deps locks, formats code, and runs the full test suite.

If anything fails, explain what went wrong and fix it before considering the work done.
